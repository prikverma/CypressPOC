import type { EyesServerSettings } from '../types';
import { type Logger } from '@applitools/logger';
import { type Req, type Options, type Fetch } from '@applitools/req';
export type ReqEyesSettings = EyesServerSettings & {
    connectionTimeout?: number;
    removeSession?: boolean;
};
export type ReqEyesOptions = Options & {
    name?: string;
    expected?: number | number[];
    logger?: Logger;
};
export type ReqEyes = Req<ReqEyesOptions>;
export declare function makeReqEyes({ settings, fetch, logger }: {
    settings: ReqEyesSettings;
    fetch?: Fetch;
    logger?: Logger;
}): Req<ReqEyesOptions>;
