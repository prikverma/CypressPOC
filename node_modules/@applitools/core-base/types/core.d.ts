import type { Core } from './types';
import { type Logger } from '@applitools/logger';
export type Options = {
    agentId?: string;
    concurrency?: number;
    cwd?: string;
    logger?: Logger;
};
export declare function makeCore({ agentId, concurrency, cwd, logger: defaultLogger, }: Options): Core;
