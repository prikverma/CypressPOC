"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.makeGetActualEnvironment = void 0;
const logger_1 = require("@applitools/logger");
const utils = __importStar(require("@applitools/utils"));
function makeGetActualEnvironment({ requests, batchingTimeout = 100, logger: mainLogger }) {
    const getActualEnvironmentsWithBatching = utils.general.batchify(getActualEnvironments, { timeout: batchingTimeout });
    return function getActualEnvironment({ settings, logger = mainLogger, }) {
        logger = logger.extend(mainLogger, { tags: [`get-actual-environment-${utils.general.shortid()}`] });
        return getActualEnvironmentsWithBatching({ settings, logger });
    };
    async function getActualEnvironments(batch) {
        const logger = (0, logger_1.mergeLoggers)(...batch.map(([{ logger }]) => logger));
        try {
            const environments = await requests.getActualEnvironments({
                settings: batch.map(([{ settings }]) => settings),
                logger,
            });
            environments.forEach((environment, index) => {
                const [, { resolve }] = batch[index];
                resolve(environment);
            });
        }
        catch (err) {
            batch.forEach(([, { reject }]) => reject(err));
        }
    }
}
exports.makeGetActualEnvironment = makeGetActualEnvironment;
