import type { UFGServerSettings } from '../types';
import { type Logger } from '@applitools/logger';
import { type Req, type Options } from '@applitools/req';
export type ReqUFGOptions = Options & {
    name: string;
    expected?: number | number[];
    logger?: Logger;
};
export type ReqUFG = Req<ReqUFGOptions>;
export declare function makeReqUFG({ settings, logger }: {
    settings: UFGServerSettings;
    logger: Logger;
}): Req<ReqUFGOptions>;
