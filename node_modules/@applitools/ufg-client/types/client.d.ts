import type { UFGClient, UFGClientSettings } from './types';
import { type Logger } from '@applitools/logger';
export declare const defaultResourceCache: Map<string, any>;
export declare function makeUFGClient({ settings, logger }: {
    settings: UFGClientSettings;
    logger?: Logger;
}): UFGClient;
