import type { Snapshot, RenderTargetSettings, RenderTarget } from './types';
import { type Logger } from '@applitools/logger';
import { type ProcessResources } from './resources/process-resources';
type Options = {
    processResources: ProcessResources;
    logger: Logger;
};
export declare function makeCreateRenderTarget({ processResources, logger: mainLogger }: Options): ({ snapshot, settings, logger, }: {
    snapshot: Snapshot;
    settings?: RenderTargetSettings | undefined;
    logger?: Logger | undefined;
}) => Promise<RenderTarget>;
export {};
